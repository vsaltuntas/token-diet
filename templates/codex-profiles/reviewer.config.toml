# Overlay: `codex --profile reviewer`
# Usage:    codex --profile reviewer
# Do not put [profiles.reviewer] in config.toml (Codex 0.147+ rejects it).
# model = "gpt-5.5"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
